---
apiVersion: v1
kind: Service
metadata:
  name: devbox-nur-itero-dev
  namespace: default
  labels:
    app: devbox-nur-itero-dev
spec:
  type: ClusterIP
  selector:
    app: devbox-nur
  ports:
    - name: default
      port: 3250
      protocol: TCP
      targetPort: 3250

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: itero-dev
  namespace: default
  annotations:
    cert-manager.io/cluster-issuer: cluster-letsencrypt-dns-cloudflare
    traefik.ingress.kubernetes.io/router.middlewares: middleware-playonjan@kubernetescrd,middleware-antimitm@kubernetescrd,sso-domainx@kubernetescrd
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: AI
    gethomepage.dev/name: Itero Dev
    gethomepage.dev/href: https://itero-dev.__{{infra.domains:f:x}}__/
    gethomepage.dev/weight: "6"
spec:
  ingressClassName: traefik
  rules:
    - host: itero-dev.__{{infra.domains:f:x}}__
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devbox-nur-itero-dev
                port:
                  name: default
  tls:
    - hosts:
        - itero-dev.__{{infra.domains:f:x}}__
      secretName: itero-dev-domainx
