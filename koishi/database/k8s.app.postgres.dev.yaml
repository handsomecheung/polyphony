---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: db-dev
  labels:
    app.kubernetes.io/name: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: postgres
    spec:
      nodeSelector:
        kubernetes.io/hostname: nur
      volumes:
        - name: data
          hostPath:
            path: /mnt/runtime-data-app/postgres.dev
            type: DirectoryOrCreate
      containers:
        - name: app
          image: postgres:18
          env:
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: "__{{database.postgres-dev.accounts:f:postgres}}__"
            - name: POSTGRES_DB
              value: default
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
          ports:
            - name: default
              containerPort: 5432
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
          livenessProbe:
            exec:
              command:
                - bash
                - -c
                - pg_isready
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 5

---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: db-dev
  labels:
    app.kubernetes.io/name: postgres
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: default
      port: 5432
      targetPort: default
