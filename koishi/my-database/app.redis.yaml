---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-redis
  namespace: my-database
  labels:
    app.kubernetes.io/name: db-redis
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: db-redis
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: db-redis
    spec:
      containers:
        - name: app
          image: redis/redis-stack-server:latest
          command: ["redis-stack-server"]
          args: ["--save", "", "--appendonly", "no"]
          ports:
            - name: default
              containerPort: 6379
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 1Gi

---
apiVersion: v1
kind: Service
metadata:
  name: db-redis
  namespace: my-database
  labels:
    app.kubernetes.io/name: db-redis
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: db-redis
  ports:
    - name: default
      port: 6379
      targetPort: default
