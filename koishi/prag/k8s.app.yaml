---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prag
  namespace: default
  labels:
    app.kubernetes.io/name: prag
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: prag
  template:
    metadata:
      labels:
        app.kubernetes.io/name: prag
    spec:
      nodeSelector:
        kubernetes.io/hostname: nur
      volumes:
        - name: data-notebook
          hostPath:
            path: /mnt/coder-workspaces/private-workspace/repos/local/notebook
            type: Directory
      containers:
        - name: app
          image: cloudpublic/default/prag:latest
          imagePullPolicy: Always
          env:
            - name: REDIS_HOST
              value: "redis.database"
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_DB
              value: "0"
            - name: OLLAMA_API_BASE
              value: "http://ollama/v1"
            - name: EMBEDDING_MODEL
              value: "bge-m3"
            - name: NOTES_PATH
              value: "/data/notebook"
          ports:
            - name: default
              containerPort: 8000
          volumeMounts:
            - name: data-notebook
              subPath: binder/Default
              mountPath: /data/notebook/binder/Default
              readOnly: true
          startupProbe:
            httpGet:
              path: /ping
              port: default
            failureThreshold: 60
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /ping
              port: default
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ping
              port: default
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 512Mi

---
apiVersion: v1
kind: Service
metadata:
  name: prag
  namespace: default
  labels:
    app.kubernetes.io/name: prag
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prag
  ports:
    - name: default
      port: 80
      protocol: TCP
      targetPort: default
