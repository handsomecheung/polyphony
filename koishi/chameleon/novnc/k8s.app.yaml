---
apiVersion: v1
kind: ConfigMap
metadata:
  name: novnc-config
  namespace: chameleon
data:
  app.conf: |
    [ui]
    show_control_bar = true
    show_extra_keys = true
    show_connection_settings = true
    show_clipboard = true
    show_fullscreen = true
    show_power = false
    show_help = true

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: novnc
  namespace: chameleon
  labels:
    app.kubernetes.io/name: novnc
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: novnc
  template:
    metadata:
      labels:
        app.kubernetes.io/name: novnc
    spec:
      volumes:
        - name: novnc-config
          configMap:
            name: novnc-config
            optional: true
      containers:
        - name: app
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          image: cloudpublic/chameleon/novnc:latest
          imagePullPolicy: Always
          env:
            - name: VNC_SERVER
              value: "glinux.chameleon:5900"
            - name: DISPLAY_WIDTH
              value: "1920"
            - name: DISPLAY_HEIGHT
              value: "1080"
            - name: WEBSOCKIFY_PORT
              value: "4567"
          ports:
            - name: default
              protocol: TCP
              containerPort: 4567
          resources:
            requests:
              cpu: 50m
              memory: 256Mi
            # TODO limit resources
            # limits:
            #   cpu: 200m
            #   memory: 512Mi
          volumeMounts:
            - name: novnc-config
              mountPath: /usr/share/novnc/config
              readOnly: true

---
apiVersion: v1
kind: Service
metadata:
  name: novnc
  namespace: chameleon
  labels:
    app.kubernetes.io/name: novnc
  annotations:
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: novnc
  ports:
    - name: default
      port: 80
      protocol: TCP
      targetPort: default

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: novnc
  namespace: chameleon
  annotations:
    cert-manager.io/cluster-issuer: cluster-letsencrypt-dns-cloudflare
    traefik.ingress.kubernetes.io/router.middlewares: sso-domaint@kubernetescrd
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Chameleon
    gethomepage.dev/href: https://novnc.__{{infra.domains:f:t}}__/vnc.html
    gethomepage.dev/siteMonitor: https://novnc.__{{infra.domains:f:t}}__/vnc.html
    gethomepage.dev/weight: "30"
spec:
  ingressClassName: traefik
  rules:
    - host: novnc.__{{infra.domains:f:t}}__
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: novnc
                port:
                  name: default
  tls:
    - hosts:
        - novnc.__{{infra.domains:f:t}}__
      secretName: novnc-domaint
