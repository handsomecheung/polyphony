---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fountain-scanner
  namespace: ck-prod
  labels:
    app.kubernetes.io/name: fountain-scanner
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fountain-scanner
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fountain-scanner
    spec:
      containers:
        - name: app
          image: cloudpublic/ck/fountain-core/scanner:latest
          imagePullPolicy: Always
          ports:
            - name: default
              containerPort: 80
          resources:
            requests:
              cpu: 30m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi

---
apiVersion: v1
kind: Service
metadata:
  name: fountain-scanner
  namespace: ck-prod
  labels:
    app.kubernetes.io/name: fountain-scanner
  annotations:
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fountain-scanner
  ports:
    - name: default
      port: 80
      protocol: TCP
      targetPort: default

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fountain-scanner
  namespace: ck-prod
  annotations:
    cert-manager.io/cluster-issuer: cluster-letsencrypt-dns-cloudflare
    gethomepage.dev/enabled: "true"
    gethomepage.dev/group: Fountain
    gethomepage.dev/name: Scanner Prod
    gethomepage.dev/href: https://fountain.__{{infra.domains:f:c}}__/scanner
    gethomepage.dev/siteMonitor: https://fountain.__{{infra.domains:f:c}}__/scanner
    gethomepage.dev/weight: "10"
spec:
  ingressClassName: traefik
  rules:
    - host: fountain.__{{infra.domains:f:c}}__
      http:
        paths:
          - path: /scanner
            pathType: Prefix
            backend:
              service:
                name: fountain-scanner
                port:
                  name: default
  tls:
    - hosts:
        - fountain.__{{infra.domains:f:c}}__
      secretName: fountain-domainc
